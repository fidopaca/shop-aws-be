export type Product = {
  id: string,
  count: number,
  description: string,
  price: number,
  title: string,
};

export type CreateProductDTO = {
  count: number,
  description: string,
  price: number,
  title: string,
};

